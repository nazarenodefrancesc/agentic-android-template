# Progress — bounded hot memory

Use one `PROGRESS-Txxx.md` per active task. Keep it short. Move durable analysis to `reports/`, durable decisions to `docs/decisions/`, and reusable operating knowledge to `skills/`.
