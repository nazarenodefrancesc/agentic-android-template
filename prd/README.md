# Task specifications

Each task is a stable Markdown specification with machine-readable front matter. Use `templates/TASK.md` and IDs `T001`, `T002`, ...

The template repository itself keeps this folder empty except for this guide; derived apps populate it from their product PRD.
