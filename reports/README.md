# Reports — cold evidence/history

Store detailed test summaries, audits, investigations and task evidence here. Generated raw tool output may go under `reports/generated/` and is ignored by Git unless intentionally promoted.
